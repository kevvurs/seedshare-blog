name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout source
        uses: actions/checkout@v1
        with:
          submodules: true
      - name: Action Hugo
        uses: kevvurs/action-hugo@v1.1-rc1
